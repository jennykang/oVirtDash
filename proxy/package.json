{
  "name": "oVirtProxy",
  "version": "0.0.0",
  "description": "Proxy server to serve the mobile dashboard along with proxying the oVirt API (To get around CORS)",
  "main": "proxy.js",
  "dependencies": {
    "http-proxy": "^1.8.1",
    "serve-static": "^1.7.1"
  }
}
